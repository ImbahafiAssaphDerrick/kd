apiVersion: v1
kind: Service
metadata:
  name: library-production
  namespace: library-app
  labels:
    app: library
spec:
  type: ClusterIP
  selector:
    app: library
    version: blue  # Switch to 'green' for blue-green deployment
  ports:
  - name: http
    port: 80
    targetPort: 3000
