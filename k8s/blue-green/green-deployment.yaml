apiVersion: apps/v1
kind: Deployment
metadata:
  name: library-green
  namespace: library-app
  labels:
    app: library
    version: green
spec:
  replicas: 3
  selector:
    matchLabels:
      app: library
      version: green
  template:
    metadata:
      labels:
        app: library
        version: green
    spec:
      containers:
      - name: library
        image: ghcr.io/imbahafiassaphderrick/kd:latest  # New version
        ports:
        - containerPort: 3000
        envFrom:
        - configMapRef:
            name: library-config
        - secretRef:
            name: library-secrets
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
